<!doctype html>
<!-- views/audit.ejs - Audit log of login attempts for Health & Fitness Tracker -->
<html>
  <head>
    <title>Audit Log - <%= shopData.shopName %></title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #f5f5f5;
      }
      .container {
        max-width: 1100px;
        margin: 0 auto;
        background: white;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
      }
      h1 {
        color: #667eea;
        text-align: center;
      }
      table {
        border-collapse: collapse;
        width: 100%;
        margin-top: 20px;
      }
      table th, table td {
        border: 1px solid #ddd;
        padding: 12px;
        text-align: left;
      }
      table th {
        background: #667eea;
        color: white;
        font-weight: bold;
      }
      table tr:hover {
        background-color: #f5f5f5;
      }
      .success {
        color: #4caf50;
        font-weight: bold;
      }
      .failed {
        color: #f44336;
        font-weight: bold;
      }
      .no-data {
        text-align: center;
        padding: 40px;
        color: #999;
      }
      .back-link {
        text-align: center;
        margin-top: 30px;
        padding-top: 20px;
        border-top: 1px solid #eee;
      }
      .back-link a {
        color: #667eea;
        text-decoration: none;
        margin: 0 15px;
      }
      .back-link a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>🔐 Audit Log - Login Activity</h1>

      <% if (audits && audits.length) { %>
        <p>Total records: <strong><%= audits.length %></strong></p>
        <table>
          <thead>
            <tr>
              <th>Time</th>
              <th>Username</th>
              <th>Status</th>
              <th>IP Address</th>
              <th>Message</th>
            </tr>
          </thead>
          <tbody>
            <% audits.forEach(function(a){ %>
              <tr>
                <td><%= new Date(a.event_time).toLocaleString() %></td>
                <td><%= a.username %></td>
                <td><span class="<%= a.success ? 'success' : 'failed' %>"><%= a.success ? '✓ Success' : '✗ Failed' %></span></td>
                <td><%= a.ip_address %></td>
                <td><%= a.message %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      <% } else { %>
        <div class="no-data">
          <p>No audit records available yet.</p>
        </div>
      <% } %>

      <div class="back-link">
        <a href="/">Home</a> | <a href="/users/list">Users</a> | <a href="/users/register">Register</a>
      </div>
    </div>
  </body>
</html>
