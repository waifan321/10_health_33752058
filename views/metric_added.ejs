<!DOCTYPE html>
<!-- views/metric_added.ejs - Confirmation after adding health metrics -->
<html>
  <head>
    <title>Metrics Logged</title>
    <link rel="stylesheet" type="text/css" href="<%= BASE_PATH %>/main.css" />
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .container {
            background: white;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.2);
            text-align: center;
            max-width: 500px;
        }
        .success-icon {
            font-size: 3em;
            margin-bottom: 20px;
        }
        h1 {
            color: #667eea;
            margin-top: 0;
        }
        .message {
            color: #333;
            font-size: 1.1em;
            margin: 20px 0;
            line-height: 1.6;
        }
        .details {
            background: #f5f5f5;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
            text-align: left;
        }
        .details p {
            margin: 5px 0;
            color: #666;
        }
        .actions {
            margin-top: 30px;
        }
        .actions a {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 12px 25px;
            text-decoration: none;
            border-radius: 5px;
            margin: 5px;
            transition: background-color 0.3s;
        }
        .actions a:hover {
            background: #764ba2;
        }
    </style>
  </head>
  <body>
    <div class="container">
        <div class="success-icon">✅</div>
        <h1>Health Metrics Logged!</h1>
        <p class="message"><%= message %></p>
        
        <div class="details">
            <p><strong>Weight:</strong> <%= weight %> kg</p>
            <p><strong>Heart Rate:</strong> <%= heartRate %> bpm</p>
            <p><strong>Steps:</strong> <%= steps %></p>
            <p><strong>Sleep:</strong> <%= sleepHours %> hours</p>
        </div>
        
        <div class="actions">
            <a href="<%= BASE_PATH %>/metrics/history">View History</a>
            <a href="<%= BASE_PATH %>/metrics/add">Log More</a>
            <a href="<%= BASE_PATH %>/">Home</a>
        </div>
    </div>
  </body>
</html>
